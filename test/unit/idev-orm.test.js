'use strict';

const assert = require('assert');
const { describe, it } = require('mocha');
const IdevOrm = require('../../lib/idev-orm.js');

module.exports = () => describe('IdevOrm', () => {
  describe('getReposiotry', () => {
    it('Returns valid repository working with db\'s pool', () => {
    });

    it('Fails on getting repository for not exisiting schema', () => {
    });
  });
});
